#ifndef PS2KEYBOARD_DRIVER_SHIM_H
#define PS2KEYBOARD_DRIVER_SHIM_H

// Shim to support old include paths that referenced "functions/drivers/..."
// This forwards to the canonical `drivers/ps2Keyboard.h` in `src/drivers`.

#include "../../drivers/ps2Keyboard.h"

#endif /* PS2KEYBOARD_DRIVER_SHIM_H */
