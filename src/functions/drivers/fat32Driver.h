#ifndef FAT32_DRIVER_SHIM_H
#define FAT32_DRIVER_SHIM_H

// Shim to support old include paths that referenced "functions/drivers/..."
// This forwards to the canonical `drivers/fat32Driver.h` in `src/drivers`.

#include "../../drivers/fat32Driver.h"

#endif /* FAT32_DRIVER_SHIM_H */
