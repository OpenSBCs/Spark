#ifndef GRAPHICS_DRIVER_SHIM_H
#define GRAPHICS_DRIVER_SHIM_H

// Shim to support old include paths that referenced "functions/drivers/..."
// This forwards to the canonical `drivers/graphicsDriver.h` in `src/drivers`.

#include "../../drivers/graphicsDriver.h"

#endif /* GRAPHICS_DRIVER_SHIM_H */
